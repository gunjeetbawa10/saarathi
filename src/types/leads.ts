export type ContactSubmissionRow = {
  id: string;
  name: string;
  email: string;
  message: string;
  created_at: string;
};

export type NewsletterSignupRow = {
  id: string;
  email: string;
  created_at: string;
};
